import 'package:flutter/material.dart';

class ProvedoresListPage extends StatelessWidget {
  const ProvedoresListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Prestadores'),
      ),
      body: Container(),
    );
  }
}
